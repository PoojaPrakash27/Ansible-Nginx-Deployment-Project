---
- name: install nginx
  ansible.builtin.apt:
    name: nginx
    state: present
- name: copy the index.html to launch it
  ansible.builtin.copy:
    src: files/index.html
    dest: /var/www/html
    mode: "0644"
